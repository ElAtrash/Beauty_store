<div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-4">
  <nav class="flex items-center space-x-2 text-sm text-muted">
    <% breadcrumbs.each_with_index do |crumb, index| %>
      <% if index > 0 %>
        <%= render IconComponent.new(name: :chevron_right, class: "w-4 h-4 text-subtle") %>
      <% end %>

      <% if crumb[:path] %>
        <%= link_to crumb[:name], crumb[:path], class: "hover:text-black transition-colors" %>
      <% else %>
        <span class="text-subtle"><%= crumb[:name] %></span>
      <% end %>
    <% end %>
  </nav>
</div>
