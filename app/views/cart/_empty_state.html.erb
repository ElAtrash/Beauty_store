<div class="empty-cart-state flex flex-col items-center justify-center h-full p-8 px-20 text-center">
  <!-- Empty Cart Icon -->
  <div class="mb-8">
    <%= render UI::IconComponent.new(name: :shopping_cart, css_class: "w-16 h-16 text-gray-300") %>
  </div>

  <!-- Empty Cart Message -->
  <h3 class="text-xl font-medium mb-2">Your cart is empty</h3>
  <p class="text-gray-500 mb-8">Discover our amazing products and add them to your cart</p>

  <!-- Action Links -->
  <div class="w-full max-w-sm space-y-4">
    <!-- Sales Link (Placeholder) -->
    <a href="#" class="block w-full bg-black text-white py-3 px-6 font-medium uppercase tracking-wide hover:bg-gray-900 transition-colors text-center">
      Browse Sales
    </a>

    <!-- Search Link (Placeholder) -->
    <a href="#" class="block w-full border border-black text-black py-3 px-6 font-medium uppercase tracking-wide hover:bg-black hover:text-white transition-colors text-center">
      Search Products
    </a>
  </div>

  <!-- Additional Info -->
  <div class="mt-12 text-sm text-gray-400">
    <p>Free shipping on orders over $50</p>
    <p>Easy returns within 30 days</p>
  </div>
</div>
