<%= render Products::CartActionsComponent.new(
    product: product,
    variant: defined?(variant) ? variant : nil,
    product_data: defined?(product_data) ? product_data : nil,
    stock_info: defined?(stock_info) ? stock_info : nil,
    current_cart: current_cart
) %>
