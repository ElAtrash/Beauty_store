<%= turbo_frame_tag "address-selection-ui",
    src: list_checkout_address_selection_path,
    loading: :lazy,
    data: { controller: "address-selector" },
    class: "flex flex-col h-full" do %>
  <!-- Content loads dynamically from list action -->
  <div class="flex items-center justify-center h-full">
    <div class="text-text-muted">Loading...</div>
  </div>
<% end %>
